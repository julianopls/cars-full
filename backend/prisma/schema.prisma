generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Carro {
  id     Int     @id @default(autoincrement())
  nome   String
  marca  String
  ano    Int
  preco  Float
  imagem String?

  @@map("carros")
}